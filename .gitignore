# OmniCode orchestration (local machine state)
.omnicode/

# Live secrets and kitchen portal — never publish
local/
.portal_key_local_only.txt
*.READY.gs
*.secret
.env
.env.*

# Kitchen login + API secrets (copy from kitchen-config.example.js)
# NEVER put real passwords in this file — .gitignore is committed to GitHub!
kitchen-config.js
**/kitchen-config.js
!kitchen-config.example.js
!**/kitchen-config.example.js

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
